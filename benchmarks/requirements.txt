# Dependencias para análisis de datos y gráficas
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
openpyxl>=3.1.0  # Para leer archivos Excel (.xlsx)

# Dependencias para Jupyter notebooks (opcional en VS Code, requerido para jupyter CLI)
ipykernel>=6.0.0
